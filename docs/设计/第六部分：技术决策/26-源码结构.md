# 源码结构

```
bugs/
├── Cargo.toml                 ← 工作区根配置
├── Cargo.lock
├── README.md
├── LICENSE
│
├── crates/
│   ├── bugs-core/             ← 核心引擎：调度/记忆/信任/场景
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── scheduler/     ← 任务队列/分配/池管理
│   │       ├── memory/        ← 记忆网格/三层存储
│   │       ├── trust/         ← 信任引擎/速率计算
│   │       ├── agent/         ← 主Agent + 子Agent
│   │       ├── scene/         ← 场景管理/切换
│   │       ├── tools/         ← 工具注册/调用
│   │       ├── browser/       ← 浏览器双池
│   │       ├── models/        ← LLM Provider 集成
│   │       ├── config/        ← 配置加载/热重载
│   │       ├── persistence/   ← redb/文件持久化
│   │       ├── security/      ← 认证/授权
│   │       └── types/         ← 共享类型定义
│   │
│   ├── bugs-api/              ← REST + WebSocket API
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── routes/        ← 端点定义
│   │       ├── middleware/     ← 认证/限流/CORS
│   │       └── schema/        ← 请求/响应 JSON Schema
│   │
│   ├── bugs-tui/              ← TUI 终端界面
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── main.rs        ← 入口，启动 TUI
│   │       ├── app.rs         ← TUI 应用状态
│   │       ├── components/    ← 对话/场景/记忆面板
│   │       └── theme.rs       ← 主题/颜色
│   │
│   ├── bugs-gui/              ← GUI 桌面界面（egui）
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── main.rs
│   │       └── ...
│   │
│   ├── bugs-web/              ← Web UI（内嵌 HTML/JS）
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── main.rs        ← HTTP 服务入口
│   │       └── static/        ← 前端资源
│   │
│   └── bugs-cli/              ← CLI 工具（bugsctl）
│       ├── Cargo.toml
│       └── src/
│           ├── main.rs
│           └── commands/      ← 子命令实现
│
├── tests/                     ← 集成测试
├── docs/                      ← 用户文档
└── .github/                   ← CI 配置
    └── workflows/
        └── ci.yml
```